# クラス関係図の設計原則

> **対象**: 全担当者（特にオンボーディング）。  
> **役割**: [`class_overview_diagram.md`](../diagrams/class_overview_diagram.md) の位置づけ・読み方と、チームが共有する設計方針の入口。§2 以降はその背景となる専門概念。

---

## 1. `class_overview_diagram.md` の位置づけ

### この図は何か

**クラス関係図**（「クラス設計図」ではない）。層間契約における **静的なクラス構造と公開 API** を、全員が一覧できる形で示す。各クラスはサブシステムとしての関係を表す。

設計レビューやオンボーディングでは、ヘッダーのシグネチャ一覧より UML 形式の方が全体像を把握しやすい。そのため **公開 API・クラス関係の事実** は本図を正とする。

### 処理フローとの違い

**処理の流れ**（受信 → パース → ディスパッチ → 返信）は本図のスコープ外。

- オンボ用の簡略図: [`reading_guide_common.md`](../onboarding_docs/reading_guide_common.md)（処理フロー図 / Mermaid flowchart）
- 詳細なデータフロー: [`data_flow_diagram.md`](../diagrams/data_flow_diagram.md)

### 層間契約の SSOT（1 契約・2 視点）

層間契約・クラス設計について、SSOT は **同じ対象** に対し **視点と目的が異なる 2 ファイル** に分かれる。矛盾ではなく役割分担である。

| 視点 | ドキュメント | 正とする内容 |
|------|-------------|-------------|
| **構造（事実）** | [`class_overview_diagram.md`](../diagrams/class_overview_diagram.md) | 公開 API・クラス関係（何があるか） |
| **意図（憲章）** | [`interface.md`](../interface.md) | 設計理由・ルール・決定事項（なぜ・どう守るか） |

**おすすめの読み順（オンボ）**: [`design.md`](../design.md) で責務分割 → 本図で構造 → 境界を実装する担当は [`interface.md`](../interface.md) の該当 §。

**更新の目安**: 公開 API や関係の変更 → 図を更新。ルール・理由・トレードオフの変更 → `interface.md` を更新。

---

## 2. SSOT (Single Source of Truth)

### 定義

情報の正規化原則。同じ**事実**が複数箇所に重複すると、更新漏れや矛盾が発生する。

### 背景

チーム開発では、設計情報が複数のドキュメントに散在しがち。例えば：

- クラス関係図
- 層間インターフェース仕様（憲章）
- オンボーディング資料・処理フロー図
- コードコメント

これらが矛盾すると、メンバーは「どれが正しいのか」を判断できず、誤った実装が生まれる。

### 適用（本プロジェクト）

§1 のとおり、層間契約は **事実は `class_overview_diagram.md`、意味は `interface.md`** と分ける。

- 矛盾発見時: **API・関係の食い違い** → 図を正として他を合わせる。**ルール・理由の食い違い** → `interface.md` を正とする
- オンボ資料やフロー図は説明用。公開 API の正は図のみ
- 議論の出発点: 構造は図、契約の「なぜ」は `interface.md`

---

## 3. UML クラス図の抽象度レベル

### Martin Fowler の3レベル [^1]

| レベル | 目的 | 記載内容 | 対象読者 |
|--------|------|----------|----------|
| **Conceptual（概念）** | ドメイン理解 | クラス名、主要な関係のみ | 全員 |
| **Specification（仕様）** | インターフェース定義 | 公開API、型、関係性 | 設計者、実装者 |
| **Implementation（実装）** | コード設計 | プライベートメンバ含む全詳細 | 実装者 |

### 適用

`class_overview_diagram.md` は **Specification レベル** として設計。

- 公開API（`+`）と関係性を記載
- プライベートメソッド（`-`）は省略
- 実装詳細（private 等）は関係図に載せず、 .hpp / .cpp で定義の予定

[^1]: Martin Fowler, "UML Distilled: A Brief Guide to the Standard Object Modeling Language", 3rd Edition, Addison-Wesley, 2003.

---

## 4. 関心の分離 (Separation of Concerns)

### 定義

各成果物は単一の関心に集中すべきという設計原則。

### 背景

一つのドキュメントに複数の関心を詰め込むと：
- 本来の目的が埋もれる
- 更新が困難になる
- 読み手が必要な情報を見つけにくい

### 適用

| 成果物 | 関心 |
|--------|------|
| `class_overview_diagram.md` | クラス間の**関係性**と**公開API** |
| `.hpp` ファイル | 実装の**インターフェース詳細** |
| `.cpp` ファイル | **実装**の詳細 |

図に全てを詰め込まず、各成果物の責務を明確に分離する。

---

## 5. インターフェースと実装の分離

### GoF の設計原則 [^2]

> "Program to an interface, not an implementation."
> （実装ではなくインターフェースに対してプログラムせよ）

### 背景

Gang of Four (GoF) は、1994年の著書「Design Patterns」で23のデザインパターンを体系化した。その中で、再利用可能な設計の原則として「インターフェースへのプログラミング」を提唱。

この原則の意図：
- クライアント [^3] は具体的な実装ではなく、抽象（インターフェース）に依存すべき

[^3]: ここでの「クライアント」は IRC クライアント（irssi 等）ではなく、オブジェクト指向設計における「利用する側のコード」を指す。例: `CommandDispatcher` は `Client` クラスの「クライアント」。
- 実装の詳細を隠蔽することで、変更の影響範囲を限定できる
- テスト容易性が向上する（モック/スタブの差し替えが可能）

### 適用

プライベートメソッドは「実装の詳細」であり、他クラスから見えない。

関係図にプライベートを記載すると：
- 本来隠蔽すべき情報が露出する
- 読み手が不適切な依存を持つ可能性がある
- 実装変更時に図の更新が必要になる

したがって、関係図には公開APIのみを記載し、プライベートは省略する。

[^2]: Erich Gamma, Richard Helm, Ralph Johnson, John Vlissides, "Design Patterns: Elements of Reusable Object-Oriented Software", Addison-Wesley, 1994. 通称「GoF本」。

---

## 6. ドキュメントのスコープ管理

### 問題

スコープを明示しないと、読み手は「何が載っていて何が載っていないか」を判断できない。

例：
- 「このメソッドが図にない。設計漏れ？」
- 「プライベートメソッドを追加すべき？」

### 対策

冒頭に「スコープ」を明記し、記載範囲を限定する。

```markdown
> **スコープ**: クラス間の関係性と公開API（`+`）のみ記載。
> プライベートメソッド（`-`）は省略。詳細は実装フェーズで決定。
```

これにより：
- 「載っていない」ことが「忘れた」のか「意図的」なかが明確
- 将来の拡張時にスコープ変更を議論できる
- 新規メンバーが図の読み方を理解できる

---

## 7. アーキテクチャ文書 vs 詳細設計文書

### 分類

| 種類 | 対象読者 | 抽象度 | 更新頻度 | 例 |
|------|----------|--------|----------|-----|
| **アーキテクチャ** | 全チーム、ステークホルダー | 高 | 低 | `class_overview_diagram.md` |
| **詳細設計** | 実装担当者 | 低 | 高 | `.hpp`、実装仕様書 |

### 適用

`class_overview_diagram.md` は **アーキテクチャ文書** として位置づける。

- 全員が共通認識を持つための「地図」
- 全ての道路名（プライベートメソッド）を載せる必要はない
- 大きな構造変更時のみ更新

---

## 8. まとめ

| 概念 | 本図への適用 |
|------|-------------|
| SSOT（2 視点） | 事実は図、`interface.md` は意図・ルール |
| UML Specification レベル | 公開APIのみ、実装詳細は省略 |
| 関心の分離 | 関係性と公開APIに集中 |
| インターフェース分離（GoF） | プライベートは隠蔽 |
| スコープ管理 | 記載範囲を冒頭に明記 |
| アーキテクチャ文書 | 全員の共通認識用「地図」 |

---

## 参考文献　AIの提案で未検証

| 書籍/資料 | 著者 | 該当トピック |
|-----------|------|-------------|
| UML Distilled (3rd Ed.) | Martin Fowler | UML の抽象度レベル |
| Design Patterns | GoF (Gamma et al.) | インターフェースへのプログラミング |
| Clean Architecture | Robert C. Martin | 関心の分離、依存性の方向 |

---

## 関連ドキュメント

| ドキュメント | 内容 |
|-------------|------|
| [class_overview_diagram.md](../diagrams/class_overview_diagram.md) | 公開 API・クラス関係（事実の SSOT） |
| [interface.md](../interface.md) | 層間契約の理由・ルール（意図の SSOT） |
| [design.md](../design.md) | 全体設計・責務分割 |
| [reading_guide_common.md](../onboarding_docs/reading_guide_common.md) | 処理フロー図（静的構造とは別） |
| [data_flow_diagram.md](../diagrams/data_flow_diagram.md) | データフロー・シーケンス |
